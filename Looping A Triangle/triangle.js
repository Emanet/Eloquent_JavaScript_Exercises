/* for(let triangle = '#'; triangle.length <= 7; triangle += '#'){
    console.log(triangle);
}
 */

let triangle = '';
while (triangle.length <= 7) {
    triangle += '#';
    console.log(triangle);
}
